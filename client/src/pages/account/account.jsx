import { Theme } from "@radix-ui/themes";
import "@radix-ui/themes/styles.css";
import Navbar from '../../components/header/header';

const Account = () => {
  return (
    <Theme>
      <Navbar />
      <h1>Account</h1>

      {/* Profile (icon)  Sign out > becomes new header */}
      {/* Goals - Add Day - Saved Meals - Macro Tracker > vertical dropdown menu on left  */}
      {/* card in middle to display choices */}
      {/* background image and styling */}

    </Theme>
  );
};

export default Account;
